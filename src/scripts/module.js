(function(window, angular, undefined) {
  'use strict';

  angular
    .module('mcCarousel', [
      'mcSlides',
      'slideBridge',
      'slideContainerBridge'
    ]);

})(window, window.angular);
